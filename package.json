{
  "name": "cypress_example_katia",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "npm run run:goals",
    "setup:env": "cp -n cypress.env.example.json cypress.env.json",
    "run:cypress": "npx cypress run --spec cypress/e2e/folders.cy.js",
    "run:goals": "npx cypress run --spec cypress/e2e/goals/goals.cy.js"
  },
  "devDependencies": {
    "cypress": "^15.17.0"
  },
  "dependencies": {
    "@faker-js/faker": "^10.4.0"
  }
}
